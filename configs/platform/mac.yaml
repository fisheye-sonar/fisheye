dataset:
  batch_size: 16
  workers: 0  # For dataloader
  use_multithreading: true
  max_workers: 2  # For multithreading

model:
  type: yolov5
  weights: weights/cfc_detect_yolov5s_v0.pt
  device: mps

inference:
  use_multithreading: true
  max_workers: 2  # For multithreading